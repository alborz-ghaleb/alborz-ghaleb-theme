## 8.9.2 — Complete PostalAddress for LocalBusiness / Organization
- هشدارهای Rich Results برای فیلدهای اختیاری آدرس برطرف شد: `streetAddress`, `addressLocality`, `addressRegion`, `postalCode`, `addressCountry`.
- آدرس‌های خالی Rank Math (`PostalAddress` بدون فیلد) و آدرس ناقص `HomeAndConstructionBusiness` قالب تکمیل می‌شوند.
- آدرس استاندارد سایت: بلوار بهشت سکینه، چهارم شرقی، پلاک ۸۰، کمالشهر / کرج / البرز / 3197963457 / IR.
- فیلتر `glass_pro/schema/postal_address` و متا/theme_modهای `glass_pro_address_*` برای سفارشی‌سازی اضافه شد.
- output buffer همه JSON-LDهای صفحه را برای PostalAddress و AggregateOffer اصلاح می‌کند.

## 8.9.1 — Product Snippets AggregateOffer (GSC Fix)
- هشدارهای Google Search Console برای Product snippets برطرف شد: `lowPrice`, `highPrice`, `offerCount` داخل `offers`.
- `AggregateOffer` ناقص با قیمت تکی معتبر به `Offer` + `price` تبدیل می‌شود.
- اگر هیچ قیمت معتبری نباشد، `AggregateOffer` به `Offer` بدون قیمت جعلی تبدیل می‌شود (جلوگیری از داده ساختگی).
- قیمت از متای برگه خوانده می‌شود: `glass_pro_agg_low_price`, `glass_pro_agg_high_price`, `glass_pro_agg_offer_count`, `glass_pro_product_price`.
- فیلترهای `glass_pro/schema/aggregate_offer_defaults` و `glass_pro/schema/offer_node` اضافه شد.
- علاوه بر فیلتر Rank Math، با output buffer تمام JSON-LDهای صفحه (از جمله اسکیمای سفارشی محتوا) اصلاح می‌شوند.

## 8.9.0 — Image Metadata Structured Data (GSC Fix)
- فیلدهای گمشده Image Metadata برای Google Search Console تکمیل شد: `license`, `creator`, `acquireLicensePage`.
- تمام `ImageObject`های خروجی Rank Math (لوگو، تصویر شاخص، primaryImageOfPage و …) به‌صورت بازگشتی غنی می‌شوند.
- `creditText` و `copyrightNotice` نیز برای سازگاری با Google Images افزوده شد.
- تصاویر خارجی (مثل Gravatar) ادعا نمی‌شوند و دست‌نخورده می‌مانند.
- URL مجوز به‌صورت پیش‌فرض به صفحه «سیاست حفظ حریم خصوصی» (در صورت وجود) و صفحه اخذ مجوز به «تماس با ما» اشاره می‌کند.
- فیلترهای `glass_pro/schema/image_license_defaults` و `glass_pro/schema/image_object` برای سفارشی‌سازی اضافه شد.
- Schema داخلی قالب (بدون Rank Math) برای لوگو و تصویر نوشته نیز تکمیل شد.

## 8.8.9 — Normal Weight View-Ad Label
- متن «مشاهده آگهی» از حالت Bold خارج شد.
- font-weight:400!important روی دکمه و span برچسب اعمال شد تا استایل عمومی یا دارک‌مود آن را ضخیم نکند.

## 8.8.8 — Clean Portfolio View Button
- متن طولانی «مشاهده: عنوان آگهی» از دکمه کارت‌های آگهی حذف شد.
- دکمه اکنون فقط برچسب «مشاهده آگهی» را داخل span اختصاصی نمایش می‌دهد.
- SVG کنار متن حذف شد تا خروجی دکمه ساده و یک‌دست باشد.
- عنوان کامل آگهی فقط در aria-label و title باقی ماند تا دسترس‌پذیری حفظ شود.
- برچسب View ad برای شش زبان قالب اضافه شد.

## 8.8.7 — Hardened Comment Spam Guard
- مسیر دورزدن ارسال ناشناس از REST API به‌طور صریح مسدود شد.
- ارسال مستقیم/سفارشی بدون فرم امضاشده قالب برای مهمانان رد می‌شود.
- چالش JavaScript به فرم افزوده شد تا ربات‌های بدون مرورگر واقعی مسدود شوند.
- IP دیگر از CF/X-Forwarded جعل‌پذیر خوانده نمی‌شود و از تابع Trusted Proxy قالب استفاده می‌کند.
- تشخیص محتوای تکراری سراسری با هش ناشناس و نگهداری 7 روزه اضافه شد.
- الگوهای رایج اسپم انگلیسی، طول غیرمجاز و تعداد خط غیرعادی مسدود می‌شوند.
- Rate Limit دو دیدگاه برای IP و هویت همچنان فعال است.
- شمارنده و هش محتوا فقط پس از درج موفق دیدگاه ثبت می‌شوند.

## 8.8.6 — Category Fix Scoped Only to Post Categories
- اصلاح مسیر فقط به taxonomy پیش‌فرض category نوشته‌های وردپرس محدود شد.
- Hookهای عمومی SEO و Pagination دیگر به‌صورت سراسری حذف نمی‌شوند.
- نرمال‌سازی Double-Encoding فقط هنگام درخواست دسته‌بندی نوشته‌ها Skip می‌شود.
- Canonical و تبدیل page/صفحه فقط برای دسته‌بندی نوشته‌ها دست‌نخورده باقی می‌ماند.
- برگه‌ها، نوشته‌های تکی، آگهی‌ها، portfolio taxonomy و سایر آرشیوها رفتار قبلی خود را حفظ می‌کنند.
- Resolver همچنان فقط get_term_by(..., category) را استفاده می‌کند و به taxonomy دیگری دست نمی‌زند.

## 8.8.5 — Category 404 & Wrong Redirect Fix
- ریدایرکت سراسری خطرناک Double-Encoding که %2520/%20 را به خط تیره تبدیل می‌کرد غیرفعال شد.
- ریدایرکت اجباری /page/ به /صفحه/ و بازنویسی Canonical مرتبط غیرفعال شد تا آرشیوها خراب نشوند.
- Resolver امن برای پایه فارسی دسته%2520بندی و گونه‌های فاصله/خط‌تیره اضافه شد.
- مسیرهای دسته‌بندی انگلیسی، روسی، عربی و ارمنی نیز در محافظ Canonical شناخته می‌شوند.
- Redirect Canonical از انتقال دسته‌بندی به صفحه نامرتبط یا خانه منع شد.
- صفحه‌بندی category با هر دو پایه page و صفحه پشتیبانی می‌شود.
- Rewrite Rules پس از نصب/به‌روزرسانی هم‌نام قالب یک بار به‌صورت خودکار Refresh می‌شوند.
- ماژول مستقل inc/category-routing-fix.php قابل حذف است.

## 8.8.4 — Professional Comment Spam Guard
- محدودیت حداکثر 2 دیدگاه در بازه شناور 24 ساعت برای هر IP اعمال شد.
- محدودیت مستقل 2 دیدگاه برای هر هویت (User ID یا ایمیل هش‌شده) اعمال شد.
- IP و ایمیل خام ذخیره نمی‌شوند؛ فقط هش HMAC کوتاه در Transient نگهداری می‌شود.
- Honeypot نامرئی برای تشخیص ربات‌های پرکننده فرم اضافه شد.
- حداقل زمان تکمیل فرم و امضای HMAC ضدجعل اضافه شد.
- فرم‌های منقضی، دستکاری‌شده، بیش‌ازحد سریع و User-Agentهای ربات مسدود می‌شوند.
- شمارنده فقط پس از درج موفق دیدگاه افزایش می‌یابد.
- مدیران/ناظران دیدگاه و Pingback/Trackback از محدودیت مستثنا هستند.
- حد روزانه با فیلتر glass_pro/comment_daily_limit قابل تغییر است.
- ماژول مستقل inc/comment-spam-guard.php قابل حذف است.

## 8.8.3 — Layout Asset Consolidation & Load-Speed Recovery
- شش درخواست CSS کوچک ماژول‌های چیدمان در یک فایل layout-optimizations.css تجمیع شدند.
- تعداد درخواست‌های CSS سفارشی از 6 به 1 کاهش یافت.
- JavaScript نوار موبایل از درخواست جداگانه حذف و به glass-bundle به‌صورت inline متصل شد.
- در مجموع 6 درخواست HTTP فرانت‌اند حذف شد (5 CSS + 1 JS).
- CSS باندل جدید minify و برای cache مرورگر نسخه‌گذاری شد.
- قابلیت حذف مستقل هر ماژول با body class اختصاصی حفظ شد؛ حذف include هر ماژول قوانین همان بخش را غیرفعال می‌کند.
- فایل‌های CSS منبع برای نگهداری و ویرایش آینده داخل قالب باقی ماندند اما جداگانه enqueue نمی‌شوند.
- کوئری‌ها، تصاویر، پرداخت، فرم‌ها و ساختار HTML تغییر نکردند.

## 8.8.2 — Language-Specific Popular Posts
- بلوک محبوب‌ترین مقالات به زبان جاری Polylang محدود شد.
- فارسی، انگلیسی، عربی، ترکی، روسی و ارمنی نتایج مستقل خود را دریافت می‌کنند.
- کش محبوب‌ترین مقالات با گروه جداگانه برای هر زبان ذخیره می‌شود.
- ترتیب همچنان بر اساس بیشترین مقدار post_views_count است.
- suppress_filters=false برای حفظ فیلتر زبان در fallback نیز اضافه شد.

## 8.8.1 — Reduced Desktop Header-to-First-Item Gap
- margin-top دسکتاپ Breadcrumb از 60px به 10px کاهش یافت.
- پدینگ بالای برگه، نوشته، آگهی و آرشیوها به 8–14px کاهش یافت.
- فاصله بالای نتایج جستجو، ریل دسته‌ها و محتوای agh-aw کاهش یافت.
- Spacer اصلی هدر ثابت دست‌نخورده ماند تا محتوا زیر هدر قرار نگیرد.
- ماژول مستقل inc/desktop-top-spacing.php و CSS اختصاصی قابل حذف است.

## 8.8.0 — Reduced Desktop Inner Content Padding
- پدینگ داخلی کارت اصلی برگه و نوشته در دسکتاپ به 24px کاهش یافت.
- پدینگ داخلی آگهی، تماس، جستجو، دیدگاه، آرشیو و کارت محصول کاهش یافت.
- بلوک‌های agh-glass، CTA، Highlight و بخش‌های اختصاصی صفحه اصلی فشرده‌تر شدند.
- ورودی‌های فرم، دکمه‌ها و سلول‌های جدول هدف قرار نگرفتند.
- عرض 768px همچنان از قوانین موبایل استفاده می‌کند؛ قوانین دسکتاپ از 769px آغاز می‌شوند.
- ماژول مستقل inc/desktop-inner-spacing.php و CSS اختصاصی قابل حذف است.

## 8.7.9 — Dedicated Mobile Layout for .agh-aw Home Content
- Hero صفحه اصلی به چیدمان منظم متن، CTA، KPI و تصویر تبدیل شد.
- سه CTA اصلی Hero به یک دکمه تمام‌عرض و دو دکمه کنار هم تبدیل شدند.
- KPIها در یک ردیف سه‌تایی فشرده و تصاویر کوچک Hero در سه ستون مرتب شدند.
- مزیت‌ها، محصولات عادی، آگهی‌های دست‌دوم، مراحل سفارش و آیتم‌های تماس دو ستونه شدند.
- محصول اصلی Feature در عرض کامل و سایر محصولات در دو ستون نمایش داده می‌شوند.
- متن‌های بلند Manifest و پنل‌های تماس برای خوانایی تک‌ستونه باقی ماندند.
- اندازه تصاویر، فونت‌ها، چیپ‌ها، کارت‌ها و فاصله‌ها برای موبایل بازتنظیم شد.
- backdrop-filter سنگین فقط در این محتوای موبایل غیرفعال شد.
- CSS با !important کنترل‌شده نوشته شد تا بر استایل توکار انتهای محتوای صفحه اصلی اولویت داشته باشد.
- ماژول مستقل inc/mobile-home-aw.php و assets/css/mobile-home-aw.css قابل حذف است.

## 8.7.8 — Two-Column Internal Content Cards on Mobile
- تمام agh-gridهای دو، سه و چهارستونه در موبایل به دو ستون کنار هم تبدیل شدند.
- قانون تک‌ستونه قبلی موبایل با override مستقل و دیرتر بارگذاری‌شونده خنثی شد.
- فاصله بین کارت‌ها به 6px و در موبایل‌های کوچک به 4px کاهش یافت.
- ارتفاع تصویر، فونت عنوان، متن، بدنه و دکمه کارت برای عرض دو ستونه بهینه شد.
- چیدمان دو ستونه حتی در عرض‌های زیر 380px حفظ می‌شود.

## 8.7.7 — Minimum Mobile Inner Content Padding
- پدینگ افقی داخل کارت‌های اصلی برگه، نوشته، آگهی، تماس، جستجو و دیدگاه به 8px کاهش یافت.
- در عرض‌های زیر 480px پدینگ داخلی بیشتر کارت‌های محتوا به 6px کاهش یافت.
- بدنه کارت‌های آرشیو و آگهی دو ستونه به 4–6px کاهش یافت.
- پدینگ افقی تصویر شاخص برگه به 3–4px کاهش یافت.
- فیلدهای فرم، دکمه‌ها و سلول‌های جدول هدف قرار نگرفتند تا قابلیت لمس و خوانایی حفظ شود.
- صفحات Elementor از override عمومی site-main مستثنا باقی ماندند.
- ماژول مستقل inc/mobile-inner-spacing.php و CSS اختصاصی قابل حذف است.

## 8.7.6 — Minimum Mobile Content Gutters
- پدینگ بیرونی محتوای صفحات در موبایل به 6px کاهش یافت.
- در عرض‌های زیر 480px گاتر بیرونی به 4px و زیر 340px به 3px کاهش یافت.
- برگه، نوشته، آگهی، آرشیو، جستجو، تماس، دیدگاه، پنل کاربر و Breadcrumb پوشش داده شدند.
- پدینگ داخلی کارت‌ها، فرم‌ها، جدول‌ها و کنترل‌ها دست‌نخورده باقی ماند تا خوانایی و عملکرد خراب نشود.
- ماژول مستقل inc/mobile-content-spacing.php و CSS اختصاصی قابل حذف است.

## 8.7.5 — Minimum Horizontal Padding for Mobile Bar
- پدینگ افقی پوسته نوار از 6 به 2 پیکسل کاهش یافت.
- پدینگ افقی هر دکمه از 7 به 2 پیکسل کاهش یافت.
- فاصله میان چهار بخش از 5 به 2 پیکسل کاهش یافت.
- در نمایشگرهای زیر 360px پدینگ و فاصله افقی به 1 پیکسل کاهش یافت.
- ارتفاع، محدوده لمس، Safe Area و چسبندگی پایین صفحه بدون تغییر باقی ماند.

## 8.7.4 — Compact Bottom-Attached Mobile Contact Bar
- ارتفاع نوار و آیتم‌ها کاهش یافت.
- نوار از حالت شناور فاصله‌دار خارج و تمام‌عرض به لبه پایین Viewport چسبانده شد.
- position: fixed به‌همراه GPU stabilization برای ماندن پایدار هنگام اسکرول اعمال شد.
- گوشه‌های پایین حذف و فقط گوشه‌های بالایی گرد باقی ماندند.
- Spacer محلی متناسب با ارتفاع جدید از 78 به 58 پیکسل کاهش یافت.
- ابعاد آیکن و فونت کمی کوچک‌تر شد؛ چهار بخش تماس، واتساپ، تلگرام و اینستا حفظ شدند.
- Safe Area آیفون و مخفی‌شدن هنگام کیبورد مجازی حفظ شد.

## 8.7.3 — Four-Part Mobile Contact Bar
- دکمه منو و کد پل Drawer از نوار پایین کاملاً حذف شد.
- نوار موبایل به چهار بخش مساوی تماس، واتساپ، تلگرام و اینستاگرام تبدیل شد.
- رنگ اختصاصی هر شبکه با استایل اصلی سایت هماهنگ شد.
- پشتیبانی چندزبانه برچسب‌های تلگرام و اینستاگرام اضافه شد.
- قابلیت مخفی‌شدن هنگام کیبورد، Safe Area، دارک‌مود و حذف FABهای تکراری حفظ شد.
- پایه نسخه همچنان 8.7.0 Performance + فقط مرحله ۴ است؛ مراحل ۱ تا ۳ وجود ندارند.

## 8.7.2 — Independent Mobile Quick Action Bar
- افزودن نوار پایین موبایل با سه اقدام تماس مستقیم، واتساپ و بازکردن Drawer منو.
- طراحی هماهنگ با رنگ‌های اصلی، دارک‌مود و Safe Area آیفون.
- مخفی شدن FABهای قدیمی فقط در موبایل برای جلوگیری از تداخل و شلوغی.
- افزودن spacer محلی پس از #page بدون تغییر padding یا ساختار کلی body.
- مخفی شدن خودکار نوار هنگام باز شدن کیبورد مجازی تا روی فیلدهای فرم قرار نگیرد.
- پشتیبانی از فارسی، انگلیسی، عربی، ترکی، روسی و ارمنی.
- نوار زیر Overlay و Drawer فعلی قرار دارد و منوی موجود را با همان دکمه همبرگر باز می‌کند.
- قابلیت کاملاً مستقل: inc/mobile-quick-bar.php به‌همراه CSS/JS اختصاصی.
- برای حذف فقط نوار، خط /inc/mobile-quick-bar.php از functions.php برداشته شود.
- پایه همچنان 8.7.0 Performance + فقط مرحله ۴ است؛ مراحل ۱ تا ۳ وجود ندارند.

## 8.7.1 — Stage 4 Only on 8.7.0 Performance Base
- این نسخه مستقیماً بر پایه 8.7.0 Performance ساخته شده است.
- هیچ‌کدام از مراحل موبایل ۱، ۲ و ۳ در این بسته وجود ندارند.
- فقط مرحله ۴ کاهش درخواست فونت فعال شده است.
- وزن CSS 500 حفظ شده ولی فایل Regular را با وزن 400 به اشتراک می‌گذارد.
- درخواست جداگانه Vazirmatn-Medium.woff2 در صفحات استفاده‌کننده از وزن 500 حذف می‌شود.
- وزن‌های 400، 600 و 700 و تمام فایل‌های اصلی فونت بدون تغییر باقی مانده‌اند.
- حذف مستقل مرحله ۴: خط /inc/mobile-stage-4.php از functions.php برداشته شود.

## 8.7.0 — ممیزی کامل Performance (Safe)
- تجمیع چهار فایل CSS سراسری هدر، فوتر، دکمه‌های شناور و کلاس‌های محتوا در یک فایل core.css.
- ادغام dark-mode-fix در dark-mode.css و حذف یک درخواست CSS سراسری.
- کوچک‌سازی محافظه‌کارانه باندل‌های CSS و JavaScript بدون تغییر API یا selectorها.
- ارائه نسخه minified استایل اصلی در حالت Standalone با fallback کامل برای Child Theme.
- انتقال CSS توکار برگه، breadcrumb و آرشیو بلاگ به فایل‌های cacheable خارجی.
- جلوگیری از بارگذاری blog-archive.css بلااستفاده روی خانه وبلاگ، دسته و برچسب.
- حذف دانلود animations.js غیرفعال و نمایش فوری عناصر reveal برای بهبود FCP/LCP.
- فقط اولین تصویر شاخص با eager/fetchpriority=high بارگذاری می‌شود؛ تصاویر مرتبط lazy هستند.
- حذف ORDER BY RAND از کوئری مطالب/آگهی‌های مرتبط و استفاده از ترتیب سریع و cache-friendly تاریخ.
- افزودن no_found_rows و گزینه‌های بهینه به کوئری‌های مرتبط.
- کش یک‌ساعته کوئری پرهزینه محبوب‌ترین نوشته‌ها.
- غیرفعال‌سازی پیش‌فرض output buffering سراسری Cloudflare که ارسال HTML را عقب می‌انداخت.
- درون‌خطی کردن تعریف فونت self-hosted برای حذف درخواست CSS کوچک فونت؛ خود فونت‌ها همچنان فقط هنگام نیاز دانلود می‌شوند.
- حفظ handleهای قدیمی CSS به‌صورت alias برای سازگاری افزونه‌ها و کدهای سفارشی.
- اعتبارسنجی نحوی ۹۱ فایل PHP، تمام CSSهای خروجی و باندل‌های JavaScript.

## 8.6.17
- بازطراحی دکمه‌های agh در موبایل با ابعاد و فونت کوچک‌تر.
- نمایش دکمه‌های ردیف CTA در دو ستون کنار هم به‌جای زیر هم.

## 8.6.16
- کاهش بیشتر فاصله زیر هدر تا بالای تصویر شاخص در موبایل.

## 8.6.15
- کاهش ارتفاع تصویر شاخص در موبایل و حذف فضای خالی زیر تصویر.
- کوچک‌تر و وسط‌چین شدن تصویر شاخص در دسکتاپ.

## 8.6.14
- کاهش فاصله زیر هدر تا تصویر شاخص در نمایش موبایل.
- کاهش فاصله بین فهرست مطالب (TOC) و محتوای اصلی در موبایل.

## 8.6.13
- هماهنگ‌سازی کامل دارک‌مود صفحه تماس با پالت اصلی قالب در همه زبان‌ها.
- حذف فیلد وب‌سایت و مسدودسازی href، تگ لینک و URL در دیدگاه‌ها.
- نمایش تصویر شاخص در تمام برگه‌ها به‌جز خانه و ترجمه‌های صفحه خانه.
- تغییر هدر برگه: تصویر عریض در بالا، عنوان کوچک‌تر در زیر تصویر.

## 8.6.12 — 2026-07-11 👤 Author Change

### Author
- Author: dashtseo.ir → M.Dasht Abadi (per user request)
- Author URI kept https://dashtseo.ir

### Version → 8.6.12

---

## 8.6.11 — 2026-07-11 🎉 Stage 52 FINAL Perfect SAFE FROM Stage 30

### 🎉 Perfect SAFE — شامل تمام مراحل امن

- Base: Stage 30 سالم (دسته-بندی/بلاگ 200)
- 31.1-37: Version, Docs, Uninstall, Cache Incrementor O(1), Notice, WebP micro-cache, TOC A11y
- 38-47: Archive perf content-visibility, Searchform A11y, PWA fallback, Footer LCP, Archive CSS external, Cities i18n, Email 24h, Zarinpal UA, Privacy, Health Check
- 48 Perfect: POT rebuild
- 49 REDO V2 SAFE: Double-encoding %2520→%20 + %20→- با Skip کامل برای بلاگ/دسته-بندی (بدون loop, بدون 404)
- 50 REDO SAFE V2: Anchor uniqueness مشاهده: عنوان
- 51 REDO SAFE V2: Link text ادامه مطلب: عنوان
- 52 FINAL: Version sync + POT date 2026-07-11 + PHPCS 0 error

### ✅ تست دسته-بندی/بلاگ
- /دسته-بندی/بلاگ/ → 200 (بدون redirect)
- /دسته%2520بندی/ → 301 → /دسته-بندی/ → 200 (Seobility fix)
- /دسته%20بندی/ → 301 → /دسته-بندی/ → 200

### 📦 Build: 168 فایل، Production clean، نسخه 8.6.11 یکسان

